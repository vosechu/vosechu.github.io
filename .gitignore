_drafts/
_site/
.DS_Store
*.swp
.jekyll-metadata

# ICRS: raw EventsAir feeds + generated data (regenerable via fetch_programme.rb;
# the presenter subset the page needs is embedded in index.html)
icrs/.data/
icrs/presenters.json
